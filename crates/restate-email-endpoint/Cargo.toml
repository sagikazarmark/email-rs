[package]
name = "restate-email-endpoint"
version = { workspace = true }
description = "Standalone endpoint hosting the email service for Restate"
edition = { workspace = true }
rust-version = { workspace = true }
license = { workspace = true }
repository = { workspace = true }
homepage = { workspace = true }
keywords = ["email", "endpoint", "restate", "queue"]
categories = ["email", "asynchronous"]

[[bin]]
name = "restate-email"
path = "src/main.rs"
required-features = ["transport-all"]
doc = false

[features]
default = ["attachment-opendal", "email-kit/default", "email-kit/tracing", "restate-email/default", "restate-email/rfc5322-string-compat", "restate-email/schemars", "transport-all"]
attachment-opendal = [
    "dep:opendal",
    "email-kit/attachment-opendal",
    "opendal/default",
    "opendal/services-azblob",
    "opendal/services-fs",
    "opendal/services-gcs",
    "opendal/services-http",
    "opendal/services-s3",
]
transport-all = [
    "attachment-opendal",
    "email-kit/serde",
    "email-kit/transport-all",
    "restate-email/service",
]

[dependencies]
email-kit = { workspace = true, default-features = false }
restate-email = { workspace = true, default-features = false }

anyhow = "1"
clap = { version = "4.5", features = ["derive", "env"] }
figment = { version = "0.10", features = ["env", "json", "toml", "yaml"] }
opendal = { workspace = true, optional = true }
serde = { workspace = true, features = ["derive"] }
restate-sdk = { workspace = true }
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
url = { version = "2", features = ["serde"] }

[dev-dependencies]
serde_json = { workspace = true }
tempfile = "3"
wiremock = "0.6"
